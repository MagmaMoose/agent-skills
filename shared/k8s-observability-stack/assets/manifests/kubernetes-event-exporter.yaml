# kubernetes-event-exporter (resmoio fork), one replica, events to stdout as JSON so the log
# collector ships them to Loki. Invariant 6: one replica and Recreate, unless leader election is on.
# Pin the image you verified. Placeholders look like <this>.
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kubernetes-event-exporter
  namespace: <namespace>
---
# The built-in `view` role: events plus read access to the objects they reference, for enrichment.
# Not a `*` wildcard, which would include every Secret in the cluster.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kubernetes-event-exporter
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: view
subjects:
  - kind: ServiceAccount
    name: kubernetes-event-exporter
    namespace: <namespace>
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kubernetes-event-exporter
  namespace: <namespace>
data:
  config.yaml: |
    logLevel: error
    logFormat: json
    # A restart must not replay old events.
    maxEventAgeSeconds: 60
    route:
      routes:
        - match:
            - receiver: stdout
    receivers:
      - name: stdout
        stdout:
          deDot: true
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-event-exporter
  namespace: <namespace>
  labels:
    app.kubernetes.io/name: kubernetes-event-exporter
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: kubernetes-event-exporter
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kubernetes-event-exporter
    spec:
      serviceAccountName: kubernetes-event-exporter
      # Watches the API. Explicit, for clusters whose policy defaults this to false.
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: kubernetes-event-exporter
          image: ghcr.io/resmoio/kubernetes-event-exporter:<version>
          args:
            - -conf=/data/config.yaml
          ports:
            - name: metrics
              containerPort: 2112
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: config
              mountPath: /data
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: kubernetes-event-exporter
---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: kubernetes-event-exporter
  namespace: <namespace>
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: kubernetes-event-exporter
  podMetricsEndpoints:
    - port: metrics
