---
name: update-docs
description: Deprecated alias for the docs-update skill. Use docs-update instead; this forwards to it so existing invocations keep working.
---

This skill was renamed to `docs-update`, so every workflow here reads `{noun}-{verb}`. The alias
exists so existing invocations keep working, and it will be removed in a future release.

Say once, before you start, that `update-docs` is deprecated and the new name is `docs-update`.
Then read `skills/docs-update/SKILL.md` and follow it in full, along with the rubric it names at
`shared/docs-update.md`.

Do not duplicate or reinterpret the workflow here. This file only forwards.
