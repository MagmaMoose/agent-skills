---
name: tvos-swiftui
description: Deprecated alias for the swiftui-build skill. Use swiftui-build instead; this forwards to it so existing invocations keep working.
---

This skill was renamed to `swiftui-build`, so every workflow here reads `{noun}-{verb}`. The alias
exists so existing invocations keep working, and it will be removed in a future release.

Say once, before you start, that `tvos-swiftui` is deprecated and the new name is `swiftui-build`.
Then read `skills/swiftui-build/SKILL.md` and follow it in full, along with the rubric it names at
`shared/swiftui-build.md`.

Do not duplicate or reinterpret the workflow here. This file only forwards.
